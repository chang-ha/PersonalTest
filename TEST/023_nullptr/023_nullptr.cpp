// 023_nullptr


int main()
{
    // nullptr
    {
        int Value = 10;

        // nullptr == 0이라고 부릅니다.
        // 0번지는 사용하지 말자고 모든 프로그래머들이 약속했기 때문에 nullptr을 만듦
        // 주소를 가리키지 않는다. 아직 아무도 가리키고 있지 않다.
        // Ptr초기화 방법 (nullptr)
        int* Ptr = nullptr;
    }
}